import axios from "axios";
import { userDetailsAPIPayload } from "@/types/api";

export const handleSignUp = (userDetails: userDetailsAPIPayload) => {
  try {
  } catch (error) {
    console.log("Sign up error : ", error);
  }
};
